const device=document.querySelector('#device'),screen=document.querySelector('#screen'),dimensions=document.querySelector('#dimensions'),page=document.querySelector('#page');let width=390,height=844;function resize(w,h){width=Number(w);height=Number(h);device.style.width=`${width}px`;device.style.height=`${height}px`;dimensions.textContent=`${width} × ${height} px`}document.querySelectorAll('[data-width]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-width]').forEach(b=>b.classList.remove('active'));button.classList.add('active');resize(button.dataset.width,button.dataset.height)}));document.querySelector('#rotate').onclick=()=>resize(height,width);page.onchange=()=>screen.src=page.value;
